[Unit]
Description=Claude Hub session reaper
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User={{ create_user }}
Group=docker
ExecStart=/home/{{ create_user }}/.cleanup-scripts/claude-hub-reaper.sh
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
