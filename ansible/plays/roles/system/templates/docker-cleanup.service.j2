[Unit]
Description=Docker system cleanup (aggressive)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User={{ create_user }}
Group=docker
ExecStart=/home/{{ create_user }}/.cleanup-scripts/docker-cleanup.sh
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
