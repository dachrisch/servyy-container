# Searxng Environment Configuration
# Managed by Ansible - do not edit manually

SEARXNG_SECRET_KEY={{ searxng_secret_key }}
SERVICE_HOST={{ searxng_service_host }}
TRAEFIK_ENTRYPOINT={{ searxng_traefik_entrypoint | default('websecure') }}
TRAEFIK_TLS={{ searxng_traefik_tls | default('true') }}
TRAEFIK_CERTRESOLVER={{ searxng_traefik_certresolver | default('letsencryptdnsresolver') }}
