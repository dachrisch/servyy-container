---
- name: Render env files for {{ service_dir }}
  template:
    src: "{{ item.src }}"
    dest: "{{ docker.remote_dir }}/{{ service_dir }}/{{ item.dest }}"
    mode: "0600"
  loop: "{{ env_templates }}"
