# Managed by Ansible - do not edit manually
PUBLIC_BASE_URL={{ 'https' if (dontforget_traefik_tls | default('true')) == 'true' else 'http' }}://{{ service_host }}
SEARXNG_BASE_URL=https://search.lehel.xyz
SEARXNG_TOKEN={{ vault_searxng_brave_token }}
OPENCODE_BASE_URL=https://opencode.{{ inventory_hostname }}
OPENCODE_API_KEY={{ opencode.api_key }}
# SMTP_* deliberately left unset — no transactional email provider chosen
# yet (docs/design.md §8 in the dontforget repo). The app falls back to
# ConsoleEmailSender, which logs the magic link to stdout instead of
# emailing it. Check `docker logs dontforget.web` to sign in until this
# is wired up.
