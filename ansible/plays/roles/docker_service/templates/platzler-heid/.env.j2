# Managed by Ansible - do not edit manually
SERVICE_HOST={{ service_host }}
SERVICE_NAME={{ service_name }}
TRAEFIK_ENTRYPOINT={{ platzler_heid_traefik_entrypoint | default('websecure') }}
TRAEFIK_TLS={{ platzler_heid_traefik_tls | default('true') }}
TRAEFIK_CERTRESOLVER={{ platzler_heid_traefik_certresolver | default('letsencryptdnsresolver') }}
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=dachrischx@gmail.com
SMTP_PASS={{ monit.smtp.password }}
SMTP_FROM=dachrischx@gmail.com
PUBLIC_BASE_URL={{ 'https' if (platzler_heid_traefik_tls | default('true')) == 'true' else 'http' }}://{{ service_host }}
