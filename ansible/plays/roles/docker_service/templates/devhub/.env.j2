# Managed by Ansible - do not edit manually
SERVICE_HOST={{ service_host }}
SERVICE_NAME={{ service_name }}
SERVICE_ROOT_HOST={{ service_root_host | default(inventory_hostname) }}
LOCAL_HOSTNAME={{ ansible_facts['hostname'] }}
TRAEFIK_ENTRYPOINT={{ devhub_traefik_entrypoint | default('websecure') }}
TRAEFIK_TLS={{ devhub_traefik_tls | default('true') }}
TRAEFIK_CERTRESOLVER={{ devhub_traefik_certresolver | default('letsencryptdnsresolver') }}
