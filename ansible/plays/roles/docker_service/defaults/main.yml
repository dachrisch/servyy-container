---
service_dir: ""
service_name: "{{ service_dir }}"
dns_short_name: ""           # Optional: override service name for DNS (e.g., "code" for opencode)
dns_expose: false            # Whether to create explicit DNS entry in Porkbun (production only)
env_templates:
  - src: docker.env.j2
    dest: .env
compose_file: docker-compose.yml
manual: false
