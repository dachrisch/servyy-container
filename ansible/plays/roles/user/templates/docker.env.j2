SERVICE_HOST={{ service.host }}
SERVICE_NAME={{ service.name }}
LOCAL_HOSTNAME={{inventory_hostname_short}}
IPV4_HOST={{ ansible_default_ipv4.address | default(lookup('dig', ansible_host)) }}
UID={{ user.id }}
GID={{ user.group }}
DOCKER_DATA_ROOT={{ extension_drive.path | default('/var/lib') }}/docker
